jQuery( document ).ready(function() {
	//	Executes any jQuery and JS that the site needs, after it loads fully
	
	//	Superfish menu
	$('ul.sf-menu').superfish();

	/*$('.bxslider').bxSlider({
		mode: 'fade',
		auto: true,
		captions: true,
		adaptiveHeight: true,
		infiniteLoop: true,
		slideWidth: 600,
		speed: 3000
	});*/

});
